[package]
name = "iso-admin-pki"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "The admin API's own certificate authority: server cert and client certs for mutual TLS"

[dependencies]
base64 = "0.22"
rcgen = { version = "0.13", features = ["x509-parser"] }
rustls-pki-types = "1"
thiserror = "2"
time = "0.3"
