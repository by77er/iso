# Copied to the control host as /var/lib/iso/secrets.toml by deploy.sh
# (SECRETS_TOML=path overrides). Values never reach a VM host or a guest.

[global."api.github.com"]
"authorization" = { bearer = "ghp_replace-me" }

[principals.alice."api.anthropic.com"]
"x-api-key" = "sk-ant-replace-me"
